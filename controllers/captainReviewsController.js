const mongoose=require('mongoose');const Rating=require('../models/Rating');const Captain=require('../models/Captain');async function list(req,res,next){try{const captain=await Captain.findOne({user:req.user._id}).select('_id rating');if(!captain){const e=new Error('Captain profile not found');e.code='CAPTAIN_NOT_FOUND';e.statusCode=404;throw e;}const limit=Math.min(Math.max(Number(req.query.limit)||20,1),100);const items=await Rating.find({captain:captain._id}).populate('customer','name profile').sort({createdAt:-1}).limit(limit).lean();res.json({success:true,data:{average:Number(captain.rating?.average||0),total:Number(captain.rating?.count||items.length),items}});}catch(e){next(e);}}module.exports={list};