const mongoose=require('mongoose');const Notification=require('../models/Notification');const {success}=require('../utils/response');async function list(req,res,next){try{const page=Math.min(Math.max(Number.parseInt(req.query.page,10)||1,1),1000000);const limit=Math.min(Math.max(Number.parseInt(req.query.limit,10)||20,1),50);const filter={recipient:req.user._id};if(req.query.unread==='true')filter.readAt=null;const [notifications,total,unread]=await Promise.all([Notification.find(filter).sort({createdAt:-1}).skip((page-1)*limit).limit(limit),Notification.countDocuments(filter),Notification.countDocuments({recipient:req.user._id,readAt:null})]);return success(res,{items:notifications,pagination:{page,limit,total,pages:Math.ceil(total/limit)},unread});}catch(e){next(e);}}async function markRead(req,res,next){try{if(!mongoose.isValidObjectId(req.params.id)){const e=new Error('Notification not found');e.statusCode=404;e.code='NOTIFICATION_NOT_FOUND';throw e;}const notification=await Notification.findOneAndUpdate({_id:req.params.id,recipient:req.user._id},{$set:{readAt:new Date()}},{new:true});if(!notification){const e=new Error('Notification not found');e.statusCode=404;e.code='NOTIFICATION_NOT_FOUND';throw e;}return success(res,notification);}catch(e){next(e);}}module.exports={list,markRead};