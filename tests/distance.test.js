const test=require('node:test');const assert=require('node:assert/strict');const {haversineKm}=require('../utils/distance');test('haversine returns zero for identical points',()=>{assert.equal(haversineKm({lat:33.5,lng:36.3},{lat:33.5,lng:36.3}),0);});test('haversine is symmetric and positive',()=>{const a=haversineKm({lat:33.5,lng:36.3},{lat:33.6,lng:36.4});const b=haversineKm({lat:33.6,lng:36.4},{lat:33.5,lng:36.3});assert.ok(a>0);assert.equal(a,b);});
