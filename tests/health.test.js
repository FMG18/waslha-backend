const test=require('node:test');const assert=require('node:assert/strict');const app=require('../server');test('health endpoint is exposed',async()=>{assert.equal(typeof app,'function');const response=await new Promise((resolve,reject)=>{const server=app.listen(0,()=>{const port=server.address().port;require('http').get(`http://127.0.0.1:${port}/health`,res=>{let body='';res.on('data',chunk=>body+=chunk);res.on('end',()=>{server.close();resolve({status:res.statusCode,body});});}).on('error',error=>{server.close();reject(error);});});});assert.equal(response.status,200);const data=JSON.parse(response.body);assert.equal(data.success,true);});