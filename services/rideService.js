const Ride=require('../models/Ride');const {haversineKm,estimateDurationMinutes}=require('../utils/distance');const {calculateFare}=require('./pricingService');const transitions={requested:['searching','cancelled'],searching:['captain_assigned','cancelled'],captain_assigned:['captain_arriving','cancelled'],captain_arriving:['captain_arrived','cancelled'],captain_arrived:['trip_started','cancelled'],trip_started:['trip_completed','cancelled'],trip_completed:[],cancelled:[]};function assertTransition(from,to){if(!transitions[from]||!transitions[from].includes(to)){const e=new Error(`Invalid ride status transition: ${from} -> ${to}`);e.statusCode=409;e.code='INVALID_RIDE_TRANSITION';throw e;}}function point(lat,lng,address=''){return{type:'Point',coordinates:[lng,lat],address};}async function createRide({customerId,serviceType,pickup,dropoff,pickupAddress='',dropoffAddress=''}){const distanceKm=haversineKm(pickup,dropoff),durationMin=estimateDurationMinutes(distanceKm),fare=calculateFare(serviceType,distanceKm,durationMin);return Ride.create({customer:customerId,serviceType,pickup:point(pickup.lat,pickup.lng,pickupAddress),dropoff:point(dropoff.lat,dropoff.lng,dropoffAddress),estimatedDistanceKm:Number(distanceKm.toFixed(2)),estimatedDurationMin:durationMin,estimatedFare:fare,status:'requested'});}async function changeStatus(ride,nextStatus,options={}){assertTransition(ride.status,nextStatus);ride.status=nextStatus;if(nextStatus==='trip_completed')ride.finalFare=ride.finalFare??ride.estimatedFare;if(nextStatus==='cancelled'){const by=['customer','captain','admin','system'].includes(options.cancelledBy)?options.cancelledBy:null;if(by)ride.cancellation.cancelledBy=by;if(options.reason!=null)ride.cancellation.reason=String(options.reason).trim().slice(0,300);ride.cancellation.cancelledAt=new Date();}return ride.save();}function canAccessRide(user,ride){if(user.role==='admin')return true;if(user.role==='customer')return ride.customer._id.toString()===user._id.toString();if(user.role==='captain'&&ride.captain)return ride.captain.user?ride.captain.user.toString()===user._id.toString():ride.captain._id.toString()===user._id.toString();return false;}module.exports={createRide,changeStatus,assertTransition,canAccessRide,transitions};