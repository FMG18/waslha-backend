const Captain=require('../models/Captain');const Ride=require('../models/Ride');const DEFAULT_RADIUS_KM=5;const MAX_CANDIDATES=20;const OFFER_TIMEOUT_MS=15000;const LOCATION_MAX_AGE_MS=Number(process.env.CAPTAIN_LOCATION_MAX_AGE_MS)||120000;function serviceToVehicleType(serviceType){return{taxi:'taxi',motorcycle:'motorcycle',delivery:'delivery'}[serviceType]||null;}async function findNearbyCaptains({ride,radiusKm=DEFAULT_RADIUS_KM,limit=MAX_CANDIDATES}){const vehicleType=serviceToVehicleType(ride.serviceType);const[lng,lat]=ride.pickup.coordinates;const maxDistanceMeters=Math.max(500,Number(radiusKm)*1000);const excluded=(ride.matching?.offeredCaptains||[]).map(id=>id.toString());const filter={status:'active',availability:'online','currentLocation.coordinates':{$exists:true},'currentLocation.updatedAt':{$gte:new Date(Date.now()-LOCATION_MAX_AGE_MS)},...(vehicleType?{'vehicle.type':vehicleType}:{})};if(excluded.length)filter._id={$nin:excluded};return Captain.find({...filter,currentLocation:{$near:{$geometry:{type:'Point',coordinates:[lng,lat]},$maxDistance:maxDistanceMeters}}}).limit(Math.min(Math.max(Number(limit)||MAX_CANDIDATES,1),MAX_CANDIDATES));}async function startMatching(rideId,options={}){const ride=await Ride.findOneAndUpdate({_id:rideId,status:'requested',captain:null},{$set:{status:'searching'}},{new:true});if(!ride){const e=new Error('Ride is not available for matching');e.statusCode=409;e.code='RIDE_NOT_MATCHABLE';throw e;}return{ride,candidates:await findNearbyCaptains({ride,...options})};}async function offerNextCaptain(rideId,options={}){for(let attempts=0;attempts<MAX_CANDIDATES;attempts+=1){const ride=await Ride.findOne({_id:rideId,status:'searching',captain:null,'matching.currentCaptain':null});if(!ride)return null;const[captain]=await findNearbyCaptains({ride,limit:1,...options});if(!captain)return null;const locked=await Captain.findOneAndUpdate({_id:captain._id,status:'active',availability:'online'},{$set:{availability:'busy'}},{new:true});if(!locked)continue;const expiresAt=new Date(Date.now()+OFFER_TIMEOUT_MS);const claimed=await Ride.findOneAndUpdate({_id:rideId,status:'searching',captain:null,'matching.currentCaptain':null},{$set:{'matching.currentCaptain':captain._id,'matching.offerExpiresAt':expiresAt},$addToSet:{'matching.offeredCaptains':captain._id}},{new:true});if(!claimed){await Captain.updateOne({_id:captain._id,availability:'busy'},{$set:{availability:'online'}});continue;}return{ride:claimed,captain:locked,expiresAt};}return null;}async function claimNextCaptain(rideId,captainId){const ride=await Ride.findOneAndUpdate({_id:rideId,status:'searching',captain:null,'matching.currentCaptain':captainId,'matching.offerExpiresAt':{$gt:new Date()}},{$set:{captain:captainId,status:'captain_assigned','matching.currentCaptain':null,'matching.offerExpiresAt':null}},{new:true});if(!ride)return null;const captain=await Captain.findOne({_id:captainId,status:'active',availability:'busy'});if(!captain){await Ride.updateOne({_id:rideId,captain:captainId,status:'captain_assigned'},{$set:{captain:null,status:'searching'}});return null;}return ride;}async function rejectCaptain(rideId,captainId){const ride=await Ride.findOneAndUpdate({_id:rideId,status:'searching','matching.currentCaptain':captainId},{$set:{'matching.currentCaptain':null,'matching.offerExpiresAt':null}},{new:true});if(ride)await Captain.updateOne({_id:captainId,availability:'busy'},{$set:{availability:'online'}});return ride;}async function expireOffer(rideId){const ride=await Ride.findOne({_id:rideId,status:'searching','matching.offerExpiresAt':{$lte:new Date()}}).select('matching.currentCaptain');if(!ride?.matching?.currentCaptain)return null;return rejectCaptain(rideId,ride.matching.currentCaptain);}module.exports={findNearbyCaptains,startMatching,offerNextCaptain,claimNextCaptain,rejectCaptain,expireOffer,serviceToVehicleType,OFFER_TIMEOUT_MS};