const express=require('express');const {register,login,bootstrapAdmin}=require('../controllers/authController');const {authRateLimiter}=require('../middleware/authRateLimiter');const router=express.Router();router.post('/register',authRateLimiter,register);router.post('/login',authRateLimiter,login);router.post('/bootstrap-admin',authRateLimiter,bootstrapAdmin);module.exports=router;