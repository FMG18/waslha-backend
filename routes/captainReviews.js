const express=require('express');const {auth,authorize}=require('../middleware/auth');const Captain=require('../models/Captain');const Rating=require('../models/Rating');const router=express.Router();router.use(auth,authorize('captain'));router.get('/',async(req,res,next)=>{try{const captain=await Captain.findOne({user:req.user._id}).select('_id');if(!captain){const e=new Error('Captain profile not found');e.statusCode=404;e.code='CAPTAIN_NOT_FOUND';throw e;}const page=Math.max(1,parseInt(req.query.page,10)||1);const limit=Math.min(100,Math.max(1,parseInt(req.query.limit,10)||20));const filter={captain:captain._id};const [items,total,stats]=await Promise.all([Rating.find(filter).populate('customer','name').sort({createdAt:-1}).skip((page-1)*limit).limit(limit).lean(),Rating.countDocuments(filter),Rating.aggregate([{$match:{captain:captain._id}},{$group:{_id:null,average:{$avg:'$score'}}}])]);const average=stats[0]?.average||0;res.json({success:true,data:{items:items.map(r=>({...r,rating:r.score})),page,limit,total,pages:Math.ceil(total/limit),averageRating:Number(average.toFixed(2))}});}catch(err){next(err);}});module.exports=router;
