const express=require('express');const {auth,authorize}=require('../middleware/auth');const controller=require('../controllers/captainRatingController');const router=express.Router();router.use(auth,authorize('captain'));router.get('/',controller.list);module.exports=router;
