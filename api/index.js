const app=require('../server');const {connectDatabase}=require('../config/database');let databaseReadyPromise=null;function ensureDatabase(){if(!databaseReadyPromise)databaseReadyPromise=connectDatabase().catch(error=>{databaseReadyPromise=null;throw error;});return databaseReadyPromise;}module.exports=async function handler(req,res){if(req.url?.split('?')[0]==='/'||req.url?.split('?')[0]==='/health')return app(req,res);try{await ensureDatabase();return app(req,res);}catch(error){return res.status(503).json({success:false,error:{code:'DATABASE_UNAVAILABLE',message:'Database connection is unavailable'}});}};