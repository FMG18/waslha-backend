const mongoose=require('mongoose');const locationSchema=new mongoose.Schema({type:{type:String,enum:['Point'],default:'Point'},coordinates:{type:[Number],required:true,validate:{validator:v=>v.length===2&&Number.isFinite(v[0])&&Number.isFinite(v[1])&&v[0]>=-180&&v[0]<=180&&v[1]>=-90&&v[1]<=90,message:'Location must contain valid [lng, lat]'}},updatedAt:{type:Date,default:Date.now}},{_id:false});const captainSchema=new mongoose.Schema({user:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true,unique:true,index:true},status:{type:String,enum:['pending','active','suspended'],default:'pending',index:true},availability:{type:String,enum:['offline','online','busy'],default:'offline',index:true},currentLocation:{type:locationSchema,default:null},vehicle:{type:{type:String,enum:['taxi','motorcycle','delivery'],default:'taxi'},make:{type:String,trim:true,maxlength:80},model:{type:String,trim:true,maxlength:80},plateNumber:{type:String,trim:true,maxlength:30}},rating:{average:{type:Number,min:0,max:5,default:0},count:{type:Number,min:0,default:0}},documentsStatus:{type:String,enum:['not_submitted','pending','approved','rejected'],default:'not_submitted'}},{timestamps:true,versionKey:false});captainSchema.index({currentLocation:'2dsphere'});captainSchema.index({status:1,availability:1,'vehicle.type':1});captainSchema.pre('validate',function(next){if(this.status!=='active'&&this.availability!=='offline')return next(new Error('Only active captains can be online or busy'));if(!this.currentLocation&&this.availability!=='offline')return next(new Error('Active captain needs a location before becoming available'));if(this.rating.count===0&&this.rating.average!==0)return next(new Error('Rating average must be zero when rating count is zero'));if(this.rating.average<0||this.rating.average>5)return next(new Error('Invalid captain rating average'));if(this.availability==='busy'&&this.status!=='active')return next(new Error('Only active captains can be busy'));next();});module.exports=mongoose.model('Captain',captainSchema);